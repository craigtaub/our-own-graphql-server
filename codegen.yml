overwrite: true
schema: "./schema.graphql"
documents: null
generates:
  artifacts/codegen.ts:
    plugins:
      - "typescript"
  # # For introspected schema file
  # artifacts/schema.json:
  #   plugins:
  #     - "introspection"
